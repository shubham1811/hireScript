Used React and Redux , Django 

we have to run both react and django

for runing react

npm install 
pipenv shell
cd hireScript
npm run dev

For Running Django
./myenv(Virtual Environment Name) / Scripts/ activate
pipenv shell
cd hirescript
cd leadmanager
python manage.py runserver

